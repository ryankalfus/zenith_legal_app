# Test Run - 2026-02-25 (Execution Pass)

## Environment prep
- `firebase --version` -> PASS (`15.7.0` after Homebrew install)
- `java -version` -> PASS (OpenJDK `25.0.2`)
- `.env` -> CREATED from `.env.example`
- Emulator toggles in `.env` -> PASS
  - `EXPO_PUBLIC_USE_FIREBASE_EMULATORS=true`
  - `NEXT_PUBLIC_USE_FIREBASE_EMULATORS=true`

## Automated checks run
- `npm install` -> PASS (engine warning only)
- `npm run typecheck` -> PASS
- `npm run build` -> PASS
- `npm run test:rules` -> FAIL/BLOCKED
  - Reason: Firestore emulator jar download failure from:
    - `https://storage.googleapis.com/firebase-preview-drop/emulator/cloud-firestore-emulator-v1.20.2.jar`

## Startup checks
- Admin: `npm run dev:admin` -> PASS (`next dev` ready on `http://localhost:3000`)
- Mobile: `npm run dev:mobile` -> FAIL (`fetch failed` in online mode)
- Mobile fallback: `EXPO_OFFLINE=1 npx expo start --offline` -> PASS (Metro started and QR served)

## Acceptance checklist execution status
- Candidate onboarding -> BLOCKED (requires running backend emulators and manual device interaction)
- Messaging -> BLOCKED (requires emulator functions + device/admin interaction)
- Firm workflow -> BLOCKED (requires emulator Firestore + manual flows)
- Appointments -> BLOCKED (requires emulator Firestore + manual flows)
- RBAC and security -> PARTIAL
  - Automated RBAC smoke harness exists at `qa/rules/rbac-smoke.mjs`
  - Could not execute due Firestore emulator download block
- Deletion -> BLOCKED (requires functions + auth emulator + manual app flow)

## Real Firebase project verification status
- `firebase --project demo-zenith-legal login:list` -> FAIL (no authorized account)
- `firebase --project demo-zenith-legal projects:list` -> FAIL (not authenticated)
- `.firebaserc` default project -> INVALID placeholder remains (`YOUR_FIREBASE_PROJECT_ID`)

## Known limitations / blockers
1. Network access to Firestore emulator jar download endpoint is failing.
2. Firebase CLI is not authenticated (`firebase login` required).
3. Real Firebase project id/secrets are not configured in `.env`.

## Next required actions
1. Ensure machine/network can download emulator jars from `storage.googleapis.com`.
2. Run `firebase login` with the target Firebase account.
3. Set valid project id (`firebase use <project-id>` and update `.firebaserc`).
4. Re-run `npm run test:rules`.
5. Run full acceptance checklist manually on emulator, then real project.

---

# Test Run - 2026-02-25 (Firebase Recovery Retry)

## Repo/config updates applied before retest
- `.firebaserc` default project changed to `zenith-legal-dev`.
- Root scripts updated:
  - `firebase:project`
  - `firebase:doctor`
  - project-aware `test:rules` (no hardcoded demo id).
- Added helper scripts under `scripts/` for project resolution, doctor checks, and rules execution.
- Added CLI helper vars to `.env.example`: `FIREBASE_PROJECT_ID`, `JAVA_HOME`, `NODE_EXTRA_CA_CERTS`.
- Updated docs: `README.md`, `docs/firebase-setup.md`.

## Preflight checks
- `npm run firebase:project` -> PASS (`zenith-legal-dev`)
- `npm run firebase:doctor` -> FAIL/WARN
  - PASS: project resolution
  - WARN: not authenticated (`firebase login` not completed)
  - FAIL: Java not on shell PATH (script now auto-falls back to Homebrew Java path during rules run)
  - WARN: `NODE_EXTRA_CA_CERTS` unset
  - WARN: emulator jar cache missing

## Automated checks
- `npm run typecheck` -> PASS
- `npm run build` -> PASS
- `npm run test:rules` -> FAIL/BLOCKED
  - New behavior: script resolves project and auto-sets Homebrew Java fallback.
  - Current blocker: Firestore emulator jar download not completing.

## TLS verification retry
- `NODE_EXTRA_CA_CERTS=/etc/ssl/cert.pem firebase setup:emulators:firestore --project zenith-legal-dev --debug` -> PARTIAL PASS
  - Result: prior TLS certificate error (`unable to get local issuer certificate`) did not occur.
  - Debug output confirms request reached jar URL and returned `HTTP 200`.
  - Download still requires completion/caching.

## Remaining blockers
1. Firebase CLI login not completed.
2. Firestore emulator jar not yet cached locally.
3. Real Firebase app config keys in `.env` still need to be filled from project settings.

## Next actions
1. Run `firebase login`.
2. Export cert env for this shell: `export NODE_EXTRA_CA_CERTS=/etc/ssl/cert.pem` (or org CA pem path).
3. Re-run `npm run test:rules` and wait for jar cache completion.
4. Continue deploy smoke checks:
   - `firebase deploy --only firestore,storage --project zenith-legal-dev`
   - `npm run build --workspace @zenith/functions`
   - `firebase deploy --only functions --project zenith-legal-dev`

---

# Test Run - 2026-02-25 (Firebase Activation + Deploy Smoke Retry)

## Login/project activation
- `firebase login --no-localhost` -> PASS
- `firebase login:list` -> PASS (authorized account present)
- `firebase projects:list` -> PASS (`zenith-legal-dev` visible)
- `firebase use zenith-legal-dev` -> PASS

## Firebase app config
- `firebase apps:create WEB zenith-legal-web --project zenith-legal-dev` -> PASS
- `firebase apps:sdkconfig WEB 1:283133081498:web:0ae37c652a17176dc3ec23 --project zenith-legal-dev` -> PASS
- `.env` Firebase public keys -> UPDATED from real project config

## Deploy smoke checks
- `firebase deploy --only firestore --project zenith-legal-dev` -> PASS
  - Included fix: removed rejected single-field index from `firestore.indexes.json`.
- `firebase deploy --only storage --project zenith-legal-dev` -> FAIL/BLOCKED
  - Reason: Storage not initialized in Firebase Console.
- `firebase deploy --only functions --project zenith-legal-dev` -> FAIL/BLOCKED
  - Reason: project must be upgraded to Blaze plan to enable Cloud Build/Artifact Registry APIs.

## Seed and emulator checks
- `npm run seed:firms` -> FAIL/BLOCKED
  - Reason: missing Application Default Credentials (`gcloud auth application-default login` needed).
- Firestore emulator jar manual fallback download -> IN PROGRESS (partial cache file growing).
- `npm run test:rules` -> PENDING until emulator jar is fully cached.

## Startup checks
- `npm run dev:admin` -> PASS (`next dev` ready on `http://localhost:3000`)
- `EXPO_OFFLINE=1 npx expo start --offline` -> PASS (`http://localhost:8081` ready)

## Next required actions
1. In Firebase Console for `zenith-legal-dev`, open Storage and click **Get started**.
2. Upgrade project to Blaze plan.
3. Install `gcloud`, run `gcloud auth application-default login`, then rerun `npm run seed:firms`.
4. Let emulator jar fallback download complete, then rerun `npm run test:rules`.

---

# Test Run - 2026-02-25 (Emulator Completion + RBAC Pass)

## Emulator dependency
- Manual fallback jar download to Firebase cache -> PASS
  - File: `~/.cache/firebase/emulators/cloud-firestore-emulator-v1.20.2.jar`
  - Size: ~117MB

## Rules validation
- `npm run test:rules` -> PASS
  - RBAC smoke summary: `10 passed, 0 failed`
  - Candidate/admin access controls validated on emulators

## Environment doctor
- `npm run firebase:doctor` -> PASS
  - 8 checks total, 0 fail, 0 warn
  - Firebase login + Java + TLS cert env + jar cache all confirmed

## Deploy retry status (post-RBAC)
- `firebase deploy --only storage --project zenith-legal-dev` -> FAIL/BLOCKED
  - Reason: Storage still not initialized in Firebase Console.
- `firebase deploy --only functions --project zenith-legal-dev` -> FAIL/BLOCKED
  - Reason: Blaze plan required before enabling Cloud Build / Artifact Registry APIs.

## Remaining gap to full acceptance
1. Storage setup in Firebase Console.
2. Blaze plan upgrade for Functions deploy.
3. ADC auth setup (`gcloud`) for real-project seed script.

---

# Test Run - 2026-02-25 (Firebase Deploy + Seed Completed)

## Deploy checks
- `firebase deploy --only storage --project zenith-legal-dev` -> PASS
  - `storage.rules` compiled and released.
- `firebase deploy --only functions --project zenith-legal-dev` -> PASS
  - Function deploy pipeline completed; unchanged functions skipped as expected.

## Seed check
- `GOOGLE_CLOUD_PROJECT=zenith-legal-dev npm run seed:firms` -> PASS
  - Result: `Imported 104 firms into Firestore.`
  - Result: `Skipped entries: Mc`

## RBAC regression check
- `npm run test:rules` -> PASS
  - RBAC smoke summary: `10 passed, 0 failed`

## Notes
- gcloud ADC auth was required for seed script and is now working in the user terminal.
- Functions deploy warns that Node.js 20 will be deprecated in 2026; runtime upgrade should be scheduled.

---

# Test Run - 2026-02-25 (Expo SDK 54 Mobile Upgrade)

## Dependency upgrade execution
- `cd apps/mobile && npx expo install expo@^54.0.0` -> PASS
- `npm install` (workspace root) -> PASS
- Mobile dependency set moved to SDK 54 compatible package versions.

## Compile and doctor checks
- `npm run typecheck --workspace @zenith/mobile` -> PASS
- `npm run typecheck` (root) -> PASS
- `cd apps/mobile && npx expo-doctor` -> PARTIAL PASS
  - Passes core SDK checks.
  - One remaining monorepo warning: duplicate `react`/`react-native` across admin/mobile workspaces.

## Runtime smoke
- `cd apps/mobile && npx expo start --lan --port 8086` -> PASS
  - Metro starts and is ready for Expo Go on SDK 54.

## Functional notes
- Auth screen currently uses email-link sign-in only.
- Phone OTP path was temporarily removed due deprecated `expo-firebase-recaptcha` package incompatibility on newer SDKs.

---

# Test Run - 2026-02-25 (Expo Go Runtime Fix)

## Issue
- Expo Go runtime crash on iOS:
  - `TurboModuleRegistry.getEnforcing(...): 'PlatformConstants' could not be found`

## Fix applied
- Added `apps/mobile/metro.config.js` to force Metro module resolution to local mobile workspace dependencies only.
- Enabled:
  - `resolver.nodeModulesPaths = [apps/mobile/node_modules]`
  - `resolver.disableHierarchicalLookup = true`

## Verification
- `cd apps/mobile && npx expo start --lan --clear --port 8087` -> PASS
  - Metro cache rebuilt successfully and bundler started without startup errors.

## Next manual check
1. Fully close Expo Go on device.
2. Restart Expo with `--clear`.
3. Scan fresh QR code and confirm app opens without the `PlatformConstants` crash.

## Follow-up fix
- Encountered bundling error after strict resolver change:
  - `Unable to resolve "expo" from apps/mobile/index.js`
- Updated Metro resolver to include:
  - `apps/mobile/node_modules` (priority)
  - root `node_modules` (fallback for hoisted packages)

---

# Test Run - 2026-02-25 (Unified Auth + Zenith Admin Enforcement Implementation)

## Dependency/install status
- `npm install` -> FAIL/BLOCKED
  - Error: `ENOTFOUND registry.npmjs.org`
  - Impact: could not install newly added mobile auth packages (`expo-auth-session`, `expo-web-browser`) in this environment.

## Code-level implementation status
- Backend updates -> COMPLETED
  - Added callable `ensureZenithAdminClaim`.
  - Added signup summary email trigger on profile completion (`users/{uid}` write).
  - Added one-time cleanup script `enforce:single-admin`.
  - Firestore rules now require admin role claim + Zenith admin email.
- Mobile updates -> COMPLETED (pending dependency install for runtime validation)
  - Replaced email-link auth UI with email/password + Google flow wiring.
  - Added admin mobile inbox screen and admin thread reply routing.
  - Candidate dashboard/status/chat/calendar/profile routes preserved for candidate role.
- Web updates -> COMPLETED
  - Added email/password + Google auth entry flow.
  - Added non-admin in-app landing route.
  - Removed recruiter-only login behavior and routed by role.

## Verification state
- `npm run build` -> PASS (shared/admin/functions)
- `npm run typecheck --workspace @zenith/shared` -> PASS
- `npm run typecheck --workspace @zenith/admin` -> PASS
- `npm run typecheck --workspace @zenith/functions` -> PASS
- `npm run typecheck` (root) -> FAIL/BLOCKED (mobile workspace missing newly added auth packages due install/network failure)
- `npm run test:rules` -> FAIL/BLOCKED (existing emulator processes on 9099/8080 already running; command cannot start duplicate emulator)
- `npm run test:rules:local` against existing emulator ports -> STARTED but did not complete inside sandbox window

## Next required local command sequence once network/port state is clear
1. `npm install`
2. `npm run typecheck`
3. Stop duplicate emulator process (or use free ports), then run `npm run test:rules`


## Localhost auth debug update (2026-02-25)
- Web auth issue reproduced: email/password and Google flows failed despite UI wiring.
- Direct API checks to Identity Toolkit returned `CONFIGURATION_NOT_FOUND`, confirming project auth config/provider setup is missing.
- Local env updated to disable app emulator mode for auth UX checks (`NEXT_PUBLIC_USE_FIREBASE_EMULATORS=false`).
- Web auth page now surfaces clearer error messages and warns if emulator mode is enabled.
- Required manual fix: initialize Firebase Authentication in console and enable Email/Password + Google providers.
- Post-fix verification: `npm run typecheck --workspace @zenith/admin` -> PASS
- Post-fix verification: `npm run build --workspace @zenith/admin` -> PASS
