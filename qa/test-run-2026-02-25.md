# Test Run - 2026-02-25

## Automated checks run
- `npm run typecheck` -> PASS
- `npm run build` -> PASS
- `npm run test:rules:local` -> EXPECTED BLOCK (fails fast with "run against Firebase emulators")

## Blocked checks
- `npm run test:rules` -> BLOCKED (`firebase` CLI not installed in this environment)

## Not yet run (needs Firebase project/emulator + device)
- Mobile runtime test (`npm run dev:mobile`)
- Full emulator RBAC run (`npm run test:rules`) after Firebase CLI install
- Push notification delivery end-to-end
- Firm seed import against real/emulator Firestore
- Full acceptance checklist in `qa/acceptance-checklist.md`

## Notes
- Added emulator-mode toggles for mobile/admin clients.
- Added RBAC smoke script (`qa/rules/rbac-smoke.mjs`) to validate candidate/admin permission boundaries.
